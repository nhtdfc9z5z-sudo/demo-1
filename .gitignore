# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local env files
.env
.env.local
.env.*.local

# Local-only Supabase access token (never commit)
.supabase_token

# Local-only Supabase DB password (never commit)
.supabase_db_password

# Local-only Vercel token (never commit)
.vercel_token
.vercel/
.vercel

# Supabase CLI local cache
supabase/.temp/
